apiVersion: v1
kind: Service
metadata:
  name: query-clusterip-service
spec:
  selector:
    app: query
  type: ClusterIP
  ports:
    - name: query-clusterip-service
      protocol: TCP
      port: 4002
      targetPort: 4002
